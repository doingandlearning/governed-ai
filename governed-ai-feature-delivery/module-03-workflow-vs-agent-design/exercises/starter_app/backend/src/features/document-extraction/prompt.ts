/** Lab 2 Task 1: keep version explicit and pass it to gateway metadata in Task 2. */
export const DOCUMENT_EXTRACTION_PROMPT_VERSION = "extract-v1";

export function buildDocumentExtractionPrompt(_inputText: string): string {
  throw new Error("Lab 2 Task 1: implement buildDocumentExtractionPrompt in prompt.ts");
}
